# Smart Surveillance System — Backend Dependencies
# Install with:  pip install -r requirements.txt
#
# NOTE: torch + torchvision are NOT listed here.
#   • GPU laptop  → install from https://pytorch.org  (CUDA version matching your driver)
#   • CPU laptop  → pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.9
websockets>=12.0

opencv-python-headless>=4.8.0
numpy>=1.24.0
timm>=0.9.0

firebase-admin>=6.2.0
PyJWT>=2.8.0
python-dotenv>=1.0.0
aiofiles>=23.2.0
